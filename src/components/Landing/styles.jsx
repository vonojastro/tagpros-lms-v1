import CD from 'components/common/ClassDetails';
import styled from 'styled-components';
export const ClassDetails = styled(CD)`
  .class-intro-text, .stars, .class-status-description, .main-info, .price, .class-action {
    display: none;
  }
`;
